#include <stdio.h>

/**
 * main - prints its name followed by newline
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 */
void main(int argc, char **argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
